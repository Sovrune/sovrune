services:
  sovrune:
    build: .
    ports:
      - "8787:8787"
    env_file:
      - path: .env
        required: false
    volumes:
      - sovrune-data:/app/data
    restart: unless-stopped

volumes:
  sovrune-data:
